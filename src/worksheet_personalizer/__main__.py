"""Entry point for running worksheet_personalizer as a module.

This allows the package to be run with:
    python -m worksheet_personalizer
"""

from worksheet_personalizer.cli import main

if __name__ == "__main__":
    main()
